# Methods added to this helper will be available to all templates in the application.
module ApplicationHelper
  # TODO: If using WhiteList and TinyMCE, uncomment these lines to override defaults.
  #WhiteListHelper.tags = %w(a strike strong em b i p pre code sub sup cite big small br div span h1 h2 h3 h4 h5 h6 ul ol li u)
  #WhiteListHelper.protocols  = %w(http https mailto)  #(ed2k ftp http https irc mailto news gopher nntp telnet webcal xmpp callto feed)

  # TODO: Likewise, the following function would be useful with WhiteList/TinyMCE:
  #def strip_all_tags(html)
  #  html.gsub(/<\/?.+?>/, '')
  #end
end
